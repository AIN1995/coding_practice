const button = document.getElementById("touroku");
const input = document.getElementById("yarukoto");

button.addEventListener("click",() =>{
    const li = dcoument.createElement("li");
    li.innertext = input.value;

   
    
});